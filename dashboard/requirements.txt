# Dashboard (Flask REST API + chat) dependencies.
#
# The dashboard also imports from scripts/shared/ (ollama_client, rag_tool,
# web_search, config, task_io), so it needs the project-root requirements too.
# Installing this file pulls both in:
#     pip install -r dashboard/requirements.txt
-r ../requirements.txt

Flask==3.1.3
Flask-Cors==6.0.5
Werkzeug==3.1.8

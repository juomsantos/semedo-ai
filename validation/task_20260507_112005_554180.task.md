---
assigned_to: coder
chain_to: qa
context_files: []
created_at: '2026-05-07T11:20:05.554180'
created_by: qa
id: task_20260507_112005_554180
original_description: Create file_watcher.py - a Python CLI tool that monitors a directory
  for new/modified files using polling, stores records (filename, size, SHA-256, timestamp)
  in SQLite, supports --query mode for table output, handles edge cases (deleted files,
  permission errors, non-existent directories), uses only standard library (os, hashlib,
  sqlite3, argparse, time)
output_path: C:\Users\JAAS\Desktop\AI Team\outbox\task_20260507_112005_554180_result.md
priority: medium
retry_count: 1
status: pending
type: code
---

## Task Description

## Original Task

Create file_watcher.py - a Python CLI tool that monitors a directory for new/modified files using polling, stores records (filename, size, SHA-256, timestamp) in SQLite, supports --query mode for table output, handles edge cases (deleted files, permission errors, non-existent directories), uses only standard library (os, hashlib, sqlite3, argparse, time)

## Previous Attempt Failed

The QA agent found the following issues:

Critical bug in `get_file_info()`: the function returns `filename` but this variable is never defined—it should be `file` (from the loop) or `os.path.basename(file_path)`. Additionally, non-existent directories are not handled (os.walk will raise FileNotFoundError that isn't caught). The query output also doesn't format as a table as implied by the task requirements.

Please fix these issues and try again.

## Expected Output

Fixed Python code that passes QA review